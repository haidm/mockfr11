<?php
$enable_slider = Mage::getStoreConfig('design/slider/enable_slider');

if (isset($enable_slider) && ($enable_slider == 1)) {
    ?>
    <?php $this->getLayout()->getBlock('head')->setTitle('My Slider'); ?>
    <!-- Link Swiper's CSS -->
    <link rel="stylesheet" href="skin/frontend/rwd/default/css/swiper.min.css">

    <!-- Demo styles -->
    <style>
        html, body {
            position: relative;
            height: 100%;
        }

        body {
            background: #eee;
            font-family: Helvetica Neue, Helvetica, Arial, sans-serif;
            font-size: 14px;
            color: #000;
            margin: 0;
            padding: 0;
        }

        .swiper-container {
            width: 100%;
            height: 400px;
        }

        .swiper-slide {
            text-align: center;
            font-size: 18px;
            background: #fff;
            /* Center slide text vertically */
            display: -webkit-box;
            display: -ms-flexbox;
            display: -webkit-flex;
            display: flex;
            -webkit-box-pack: center;
            -ms-flex-pack: center;
            -webkit-justify-content: center;
            justify-content: center;
            -webkit-box-align: center;
            -ms-flex-align: center;
            -webkit-align-items: center;
            align-items: center;
        }

        .swiper-pagination-bullet {
            width: 20px;
            height: 20px;
            text-align: center;
            line-height: 20px;
            font-size: 12px;
            color: #000;
            opacity: 1;
            background: rgba(0, 0, 0, 0.2);
        }

        .swiper-pagination-bullet-active {
            color: #fff;
            background: #007aff;
        }
        .imageSlider{
            position: relative;
        }
        .description{
            position: absolute;
            bottom:20px;
            left: 30px;
            font-size: 24px;
            color: white;
            text-transform: capitalize;
            text-shadow: 3px 2px green;

        }
    </style>
    <!-- Swiper -->
    <div class="swiper-container">
        <div class="swiper-wrapper">
            <?php
            $data = $this->getListDataSlider();
            $dirImg = array();
            foreach ($data as $value) {
                $dirImg = Mage::getBaseDir() . str_replace("/", DS, strstr($value['name_image'], '/media'));
                if (file_exists($dirImg)) {
                    ?>
                    <div class="swiper-slide imageSlider"><img width="100%" height="100%"
                                                   src="<?php echo $value['name_image']; ?>"
                                                   alt="<?php echo $value['description']; ?>"
                        />
                        <span class="description"><strong><?php echo $value['description']; ?></strong></span>
                    </div>
                    <?php
                }
            }
            ?>
        </div>
        <!-- Add Pagination -->
        <div class="swiper-pagination"></div>
    </div>


    <!-- Swiper JS -->
    <script src="skin/frontend/rwd/default/js/swiper.min.js"></script>

    <!-- Initialize Swiper -->
    <!--    Pagination Slider   -->
    <?php
    $typeSlider = Mage::getStoreConfig('design/slider/select_slider');
    if ($typeSlider == 1) {
        ?>
        <script>
            var swiper = new Swiper('.swiper-container', {
                pagination: {
                    el: '.swiper-pagination',
                },
            });
        </script>
        <!--    Navigation Slider   -->
    <?php } else if ($typeSlider == 2) { ?>
        <script>
            var swiper = new Swiper('.swiper-container', {
                navigation: {
                    nextEl: '.swiper-button-next',
                    prevEl: '.swiper-button-prev',
                },
            });
        </script>
        <!--    Custom Pagination Slider   -->
    <?php } else if ($typeSlider == 3) { ?>
        <script>
            var swiper = new Swiper('.swiper-container', {
                pagination: {
                    el: '.swiper-pagination',
                    clickable: true,
                    renderBullet: function (index, className) {
                        return '<span class="' + className + '">' + (index + 1) + '</span>';
                    },
                },
            });
        </script>
    <?php }
} else { ?>
<div class="slideshow-container">
    <ul class="slideshow">
        <li><a href="http://magedev.local/accessories/eyewear.html"><img src="wysiwyg/slide-1.jpg" alt="An eye for detail - Click to Shop Eye Wear" /></a></li>
        <li><a href="http://magedev.local/women.html"><img src="wysiwyg/slide-2.jpg" alt="Style solutions - covet-worthy styles in travel-friendly fabrics - Click to Shop Woman" /></a></li>
        <li><a href="http://magedev.local/men.html"><img src="wysiwyg/slide-3.jpg" alt="Wing man - hit the runway in stylish separates and casuals - Click to Shop Man" /></a></li>
    </ul>
    <div class="slideshow-pager">&nbsp;</div>
    <span class="slideshow-prev">&nbsp;</span> <span class="slideshow-next">&nbsp;</span></div>
<?php }


